#import "@preview/fontawesome:0.5.0": *

== Projects
#line(length: 100%)

#block(breakable: false)[
  #stack(dir: ltr, spacing: 1fr)[
    === LinkedIn Greeter
  ][
    #link("https://github.com/GrayHat12/lnkd-llm-service")[
      #fa-icon("github")
    ]
  ]
  _Hiring Assignment_
  - *Scalable solution* with *cost analysis* for a service that given a linkedin profile, *scrapes* their last 5 posts and uses a *Large Language Model* to generates a customised connection request.
  Skills: *Python, Docker, MongoDB, NodeJS, RabbitMQ, Ollama, Puppeteer, FastAPI*
]

#v(2pt)

#block(breakable: false)[
  #stack(dir: ltr, spacing: 1fr)[
    === JSON Comparison
  ][
    #link("https://github.com/GrayHat12/json-ops")[
      #fa-icon("github")
    ]
    #h(5pt)
    #link("https://grayhat12.github.io/json-ops/")[
      #fa-icon("globe")
    ]
  ]
  _Work Inspired_
  - I saw the need of a local, *efficient JSON comparison* tool while working at Karza. The existing solutions were struggling to handle large JSON files.
  - This solutions is a *PWA* that utlises *worker threads* on your browser to offload the JSON parsing and comparison tasks so as to not crash your browser tab when you try to compare large JSON files.
  Skills : *ReactJS, Node Workers, IndexDB, PWA*
]

#v(2pt)

#block(breakable: false)[
  #stack(dir: ltr, spacing: 1fr)[
    === PymHash
  ][
    #link("https://github.com/GrayHat12/pymhash")[
      #fa-icon("github")
    ]
    #h(5pt)
    #link("https://grayhat12.github.io/pymhash")[
      #fa-icon("book")
    ]
  ]
  _Personal Project_
  - Simple efficient Image Hashing Library which can be used to identify or categorise similar images.
  Skills : *Rust, Python, Maturin*
]

#v(2pt)

#block(breakable: false)[
  #stack(dir: ltr, spacing: 1fr)[
    === wtf
  ][
    #link("https://github.com/GrayHat12/wtf")[
      #fa-icon("github")
    ]
  ]
  _Personal Project_
  - A CLI tool to quickly define things.
  *Skills* : *Golang, Goroutines, Wikipedia API*
]